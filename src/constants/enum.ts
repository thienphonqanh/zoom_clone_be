export enum UserVerifyStatus {
  Unverified,
  Verrified,
  Banned
}

export enum TokenType {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  EmailVerifyToken
}

export enum ScheduleStatus {
  Scheduled,
  Ongoing,
  Completed,
  Canceled
}
